# Node Auth Afternoon - Solution

Steps
* `npm i`
* Copy `.env.example` to `.env` and fill out values. Read the README in the `master` branch for instructions on how to get values from the Auth0 service.
* `nodemon`
* `npm start`
